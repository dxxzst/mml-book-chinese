# 《机器学习中的数学》中文版翻译与排版指南（避坑手册）

本文档总结了本项目在实际翻译与排版推进中验证确立的核心规范、踩坑经验与解决方案，供后续章节翻译及协作时参考遵循。

---

## 一、工具链与可用技能体系

在翻译与图表制作过程中，建议充分利用以下工具组合：

### 1. 原文检索与提取
- **原版 PDF 路径**：`E:\需求记录\文档资料\Books\mml-book_printed.pdf`（或对应环境本地书籍路径）。
- **`document-skills:pdf` + `uv` 环境**：
  - 本项目通过 `uv run --with pymupdf` 调用 PyMuPDF，可精确检索原书关键词、定位具体章节页码、提取纯文本与数学公式；
  - 原书大多插图为矢量绘制（PDF 无独立内嵌位图），通过 PyMuPDF 以 4x 高倍分辨率（`fitz.Matrix(4.0, 4.0)`）对页面指定图表区域进行超清栅格化，随后利用 Pillow 进行边缘清洗与透明化裁剪。

### 2. 图像汉化与重绘
- **Pillow / Matplotlib**：用于对截取出的图表做去底色、尺寸规整、文本层汉化重绘，保存为无损 PNG。

---

## 二、KaTeX / VS Code 预览排版三大硬性红线

VS Code 的 Markdown 预览扩展（如 Markdown All in One / Markdown Preview Enhanced）采用 KaTeX 进行公式渲染，对语法边界极其敏感，**绝不能套用宽松的 LaTeX 或网页容错逻辑**：

### 🚨 避坑 1：块级公式定界符 `$$` 必须前后独立成行
- **错误现象**：抛出 `ParseError: KaTeX parse error: Can't use function '$' in math mode at position ...`。
- **根本原因**：若行末闭合的 `$$` 紧接着跟中文（例如 `... \tag{2.33} $$ 那么对于任何 $ \lambda $ 我们得到 $$`），KaTeX 解析器会将整行当作**未闭合的数学环境**，在数学环境内再次遇到行内公式 `$` 时即报错崩溃。
- **正确规范**：所有 `$$` 必须独立换行成段，绝不能与中文或普通标点连在同一行。

### 🚨 避坑 2：单个 `$$ ... $$` 块内严禁出现多个 `\tag{...}`
- **错误现象**：抛出 `ParseError: KaTeX parse error: Multiple \tag`。
- **根本原因**：KaTeX 严格限制一个独立的块级公式环境（一对 `$$`）内最多只能有一个 `\tag`。即使在 `\begin{aligned}` 或 `\begin{array}` 内换行，多个 `\tag` 也会被直接拒识。
- **正确规范**：若多行推导需分别赋予子编号（如 `(2.34a)` 与 `(2.34b)`），**必须拆分为多个独立的 `$$ ... \tag{...} $$` 块**：
  ```markdown
  > $$ (\lambda + \psi)\boldsymbol C = \begin{bmatrix} ... \end{bmatrix} \tag{2.34a} $$
  > $$ = \begin{bmatrix} ... \end{bmatrix} + \begin{bmatrix} ... \end{bmatrix} = \lambda \boldsymbol C + \psi \boldsymbol C \tag{2.34b} $$
  ```

### 🚨 避坑 3：禁止出现连续的空公式标记 `$$ $$`
- **根本原因**：连续的 `$$ $$` 会使解析器的数学模式开闭标志位发生翻转，导致其后整段正文被当成未闭合的公式吃入。

---

## 三、术语中英双语与字体强调规范

### 🚨 避坑 4：下划线 `_` 表示斜体时两侧必须保留半角空格
- **错误现象**：术语无法显示为特殊字体（斜体），而是原样输出了下划线，如 `_向量空间（vector spaces）_`。
- **根本原因**：CommonMark / GFM 具有“词内下划线（Intra-word underscore）保护”机制。当 `_` 紧贴汉字或全角标点时，解析器认为其处于词语中间，不会作为强调处理。
- **正确规范**：
  ```markdown
  <!-- ❌ 错误：紧贴中文字符，斜体失效 -->
  探讨_向量空间（vector spaces）_，即向量所处的...
  我们首先介绍_群（group）_的概念

  <!-- ✅ 正确：前后保留半角空格 -->
  探讨 _向量空间（vector spaces）_ ，即向量所处的...
  我们首先介绍 _群（group）_ 的概念
  ```

---

## 四、插图与图题规范

### 🚨 避坑 5：HTML 图题标签 `<b>` 内严禁使用 `$ ... $` 公式
- **错误现象**：图题里的数学符号无法渲染，直接显示为原始字面量，如 `图 2.6 并非 $ \mathbb{R}^2 $ 的所有子集都是子空间`。
- **根本原因**：处于 `<p align="center"><b>...</b></p>` 纯 HTML 标签内部的文本会被 Markdown 解析器跳过，不执行 LaTeX 公式转换。
- **正确规范**：
  - 图片存放路径：统一保存在 `docs/images/FigureX.Y.png`；
  - 引用必须使用**相对路径**（`../../docs/images/...`），坚决不用 GitHub 绝对外链；
  - HTML 居中图题内涉及上标或集合符号时，统一使用 **标准 Unicode 字符**（如 `R²`）：
  ```html
  <p align="center">
    <img src="../../docs/images/Figure2.6.png" alt="图 2.6 并非 R² 的所有子集都是子空间。在 A 和 C 中违背了封闭性；B 不包含 0。只有 D 是子空间。" title="图 2.6 并非 R² 的所有子集都是子空间。在 A 和 C 中违背了封闭性；B 不包含 0。只有 D 是子空间。" /><br>
    <b>图 2.6 并非 R² 的所有子集都是子空间。在 A 和 C 中违背了封闭性；B 不包含 0。只有 D 是子空间。</b><br>
  </p>
  ```

---

## 五、目录与文件链接规范

- **去中心化与离线化**：所有文章链接（特别是 `README.md` 中的目录导航）必须使用相对路径 `./...`，避免使用 `https://github.com/...` 链接，确保本地离线阅读与跨平台克隆无缝可用。
- **进度追踪**：每翻译完一个章节或小节，及时在 `README.md` 的对应小节打勾 `[x]`。
